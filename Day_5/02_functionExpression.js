// Task 3: Calculate the sum 
const maxNumber = (num1, num2) => {
  return num1 > num2 ? `${num1} is greater` : `${num2} is greater`;
};

console.log(maxNumber(10, 20));

// Task 4: Check if string contains specific character
const concateString = (str1, str2) => {
  return `${str1} ${str2}`;
};

console.log(concateString("Ankush", "Thakur"));
